# Saturday-Review-of-Python-basics
Just what I have learnt within the week! It'll only get better!
I have started this week Studying Python from tha basics again understaind the syntax, the language itself then further through  the year youll see more projects and actual full applications built using Python!


### Variables
- Learning about Variables, this has been useful in regards to storing data(strings,floats & integers), instead of writing out the same code over and over again
  its a time saver and helps to keep code clean and readable.
- Using discriptive & logical names for the Variables is another important factor espcially when i start writing code for other and contriubuting to bigger teams as    such.
- you can concaternate using the + operator to add to bits together
- one inportant thing with concaternate, if using numbeer we must convert it using a str()function so * newAge=100 print("Hello there i am " + str(newAge) + " years old!")

### str() and int() and float()
- using these functions help to convert data types
- houseAge = 100 ; print("hello BOB, how old is your house " + str(houseAge) + " right, thats pretty old for a house")
using float() we can change an integer(whole number) into float or using int() to turn float(decimal number ) into an integer

### MFunctions print() and input()
- Both print and input are functions and to be more precise they are built-in functions, they are ready for us to use whenever!
- Now the print function simply logs outcome into the console good in getting feed back and testing if something is correct.
- Interestingly input() is another function that allows user input, another interesting snippet, is that you can put the input into a variable and then
  reuse that data that was inputted into the print function.

  ### Basic Arthemtic
  - PEMDAS
  - Parentheses
  - Exponents (Power of)
  - Multiplication / Division
  - Addition / Subtraction
  - Just like in math, Multiplication/Division have the same priority, and Addition/Subtraction have the same priority. If Python sees both in a row, it solves them      from left to right.

  #### little tip
  - If i or you, ever want to write two lines of code on the same line i would use *;* so newAge = 100; print("Hello there i am " + str(newAge) + " years old!") :)
 
  ### Round up
  - I enjoyed this week i will keep practing throught the weekend regardless, i want to get to know more aabout python and cfeat better and bigger thhings! Thank you for      reading!
